{% extends 'admin/base.html.twig' %}

{% block body %}

    <div class="row justify-content-center">
        <div class="col-sm-12">
            <div class="card">
                <div class="card-body">
                    <div class="card-content">
                        {{ form(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('form') }}
    {# TODO Rework added files #}
    {# <script src="{{ asset('assets/main/js/jQuery.tagify.min.js') }}"></script> #}
    {# <script src="{{ asset('assets/admin/js/category/form.js') }}"></script> #}
{% endblock %}